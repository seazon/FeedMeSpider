<manifest package="com.seazon.feedme.spider.yidian"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <service android:name="com.seazon.feedme.spider.yidian.SpiderService">
            <intent-filter>
                <action android:name="com.seazon.feedme.spider.Extension"/>
            </intent-filter>
        </service>
    </application>

</manifest>
